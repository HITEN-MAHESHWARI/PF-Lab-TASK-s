#include <stdio.h>

int main() {
	
	int unit;
	printf("Enter the total consumed units: ");
	scanf("%d", &unit);
	
	if(unit <= 100)
	{
		printf("Low usage");
	}
	else if(unit >= 101 && unit <= 300)
	{
		printf("Medium usage");
	}
	else
	{
		printf("High usage");
	}
	return 0;
}
