#include <stdio.h>
int main() 
{	
	int attendence;
	printf("Enter the attendance: ");
	scanf("%d", &attendence);
	
	if(attendence >= 75)
	{
		printf("Eligible for exam");
	}
	else
	{
		printf("Not eligible for exam");
	}
	return 0;
}
